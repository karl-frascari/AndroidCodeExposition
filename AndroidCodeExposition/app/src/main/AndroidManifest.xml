<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="karl.androidcodeexposition">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FragmentsActivity" />
        <activity android:name=".CanvasActivity" />
        <activity android:name=".Activity_A" />
        <activity android:name=".Activity_B" />
        <activity android:name=".Activity_C" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login" />
        <activity android:name=".ProcessLoginActivity" />
        <activity android:name=".OlimipiadasHomeActivity" />
        <activity android:name=".OlimipiadasConfirmationActivity" />
        <activity android:name=".AlarmActivity" />

        <receiver android:name=".AlarmReceiver" />

        <activity android:name=".ServiceActivity" />

        <service
            android:name=".TesteService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".BoundActivity" />

        <service
            android:name=".BoundService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>